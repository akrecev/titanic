version: '3.8'
services:

  db:
    image: postgres:12-alpine
    container_name: titanic_db
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=titanic
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password